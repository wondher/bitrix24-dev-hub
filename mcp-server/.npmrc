# Route the @wondher scope to the GitHub Packages registry.
# The auth token must live in the user's ~/.npmrc (never committed):
#   //npm.pkg.github.com/:_authToken=YOUR_GITHUB_PAT
@wondher:registry=https://npm.pkg.github.com
